apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: nlp-fastapi-ingress
  namespace: default
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /upload
        pathType: Prefix
        backend:
          service:
            name: nlp-fastapi-service
            port:
              number: 80