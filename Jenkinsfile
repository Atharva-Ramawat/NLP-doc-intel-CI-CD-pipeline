pipeline {
    agent any
    stages {
        stage('Test Connection') {
            steps {
                echo '✅ SUCCESS: Jenkins has successfully pulled this file from GitHub!'
            }
        }
    }
}